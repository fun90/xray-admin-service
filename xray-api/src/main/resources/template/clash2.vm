mixed-port: 7890
allow-lan: false
mode: rule
log-level: info
ipv6: false
external-controller: 0.0.0.0:9090

dns:
  enable: true
  listen: 127.0.0.1:5311
  ipv6: false
  default-nameserver:
    - 223.5.5.5
    - 119.29.29.29
  nameserver:
    - 223.5.5.5
    - 119.29.29.29
#当dns解析出现问题时，打开如下配置
#  fallback:
#    - 自己的DNS
#  fallback-filter:
#    geoip: false
#    geoip-code: CN
#    ipcidr:
#      -192.168.0.1/16
#    domain:
#      - 需要走自己的DNS的域名，如：'*.test.com'

proxies:
#foreach($server in $servers)
  #if($server.protocol == "vmess")
  - {name: $server.serverName, server: $server.clientDomain, port: $server.clientPort, type: vmess, uuid: $account.uuid, alterId: $account.maxConnection, cipher: auto, tls: true, skip-cert-verify: false, network: ws, ws-opts: {path: $server.wsPath, headers: {Host: $server.clientDomain}}, udp: false}
  #elseif($server.protocol == "trojan")
  - {name: $server.serverName, server: $server.clientDomain, port: $server.clientPort, type: trojan, password: $account.uuid, sni: $server.clientDomain, skip-cert-verify: false, udp: false}
  #else
  #end
#end

proxy-groups:
  - name: 🔰 代理
    type: select
    proxies:
      - ♻️ 自动选择
      - 🎯 直连
    #foreach($server in $servers)
      #if($server.protocol == "vmess" || $server.protocol == "trojan")
      - $server.serverName
      #end
    #end

  - name: ♻️ 自动选择
    type: url-test
    url: http://www.gstatic.com/generate_204
    interval: 300
    proxies:
    #foreach($server in $servers)
      #if($server.protocol == "vmess" || $server.protocol == "trojan")
      - $server.serverName
      #end
    #end

  - name: 💰 PayPal
    type: select
    proxies:
      - 🎯 直连
      - 🔰 代理
      - ♻️ 自动选择
    #foreach($server in $servers)
      #if($server.protocol == "vmess" || $server.protocol == "trojan")
      - $server.serverName
      #end
    #end

  - name: 🐶 谷歌
    type: select
    proxies:
      - 🔰 代理
      - ♻️ 自动选择
    #foreach($server in $servers)
      #if($server.protocol == "vmess" || $server.protocol == "trojan")
      - $server.serverName
      #end
    #end

  - name: 🎬 YouTube
    type: select
    proxies:
      - 🔰 代理
      - ♻️ 自动选择
    #foreach($server in $servers)
      #if($server.protocol == "vmess" || $server.protocol == "trojan")
      - $server.serverName
      #end
    #end

  - name: Ⓜ️ 微软
    type: select
    proxies:
      - 🎯 直连
      - 🔰 代理
      - ♻️ 自动选择
    #foreach($server in $servers)
      #if($server.protocol == "vmess" || $server.protocol == "trojan")
      - $server.serverName
      #end
    #end

  - name: ☁️ iCloud
    type: select
    proxies:
      - 🎯 直连
      - 🔰 代理
      - ♻️ 自动选择
    #foreach($server in $servers)
      #if($server.protocol == "vmess" || $server.protocol == "trojan")
      - $server.serverName
      #end
    #end

  - name: 🍎 苹果
    type: select
    proxies:
      - 🎯 直连
      - 🔰 代理
      - ♻️ 自动选择
    #foreach($server in $servers)
      #if($server.protocol == "vmess" || $server.protocol == "trojan")
      - $server.serverName
      #end
    #end

  - name: 🎯 直连
    type: select
    proxies:
      - DIRECT

  - name: 🛑 拦截
    type: select
    proxies:
      - REJECT
      - DIRECT

  - name: 🐟 其他
    type: select
    proxies:
      - 🎯 直连
      - 🔰 代理
      - ♻️ 自动选择
    #foreach($server in $servers)
      #if($server.protocol == "vmess" || $server.protocol == "trojan")
      - $server.serverName
      #end
    #end

rules:
  - RULE-SET,Applications,🎯 直连
  - RULE-SET,MyReject,🛑 拦截
  - RULE-SET,MyProxy,🔰 代理
  - RULE-SET,MyDirect,🎯 直连
  - RULE-SET,PayPal,💰 PayPal
  - RULE-SET,YouTube,🎬 YouTube
  - RULE-SET,Google,🐶 谷歌
  - RULE-SET,Microsoft,Ⓜ️ 微软
  - RULE-SET,iCloud,☁️ iCloud
  - RULE-SET,Apple,🍎 苹果
  - DOMAIN,clash.razord.top,🎯 直连
  - DOMAIN,yacd.haishan.me,🎯 直连
  - RULE-SET,Private,🎯 直连
##  - RULE-SET,Reject,🛑 拦截
##  - RULE-SET,AdvertisingLite,🛑 拦截
##  - RULE-SET,AdvertisingLiteDomain,🛑 拦截
  - RULE-SET,BanProgramAD,🛑 拦截
  - RULE-SET,Tld-not-cn,🔰 代理
  - RULE-SET,Gfw,🔰 代理
  - RULE-SET,Greatfire,🔰 代理
  - RULE-SET,TelegramCidr,🔰 代理
  - MATCH,🐟 其他

#set($rulesNameList = [
  "MyReject",
  "MyProxy",
  "MyDirect",
  "PayPal",
  "Google",
  "YouTube",
  "Microsoft",
  "iCloud",
  "Apple",
  "Applications",
  "Private",
  "BanProgramAD",
  "Tld-not-cn",
  "Gfw",
  "Greatfire",
  "TelegramCidr"
])
rule-providers:
##  AdvertisingLite:
##    type: http
##    behavior: classical
##    url: "$rootUrl/subscribe/rules/clash2/AdvertisingLite.list"
##    path: ./ruleset/AdvertisingLite.yaml
##    interval: 86400
##
##  AdvertisingLiteDomain:
##    type: http
##    behavior: domain
##    url: "$rootUrl/subscribe/rules/clash2/AdvertisingLiteDomain.list"
##    path: ./ruleset/AdvertisingLiteDomain.yaml
##    interval: 86400
##
#foreach( $rulesName in $rulesNameList)
  $rulesName:
    type: http
    behavior: classical
    url: "$rootUrl/subscribe/rules/clash2/${rulesName}.list"
    path: ./ruleset/${rulesName}.yaml
    interval: 86400

#end